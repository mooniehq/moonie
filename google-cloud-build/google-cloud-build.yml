steps:

# Decrypt the file containing the key
# - name: 'gcr.io/cloud-builders/gcloud'
#   args:
#   - kms
#   - decrypt
#   - --ciphertext-file=id_rsa.enc
#   - --plaintext-file=/root/.ssh/id_rsa
#   - --location=global
#   - --keyring=moonie-keyring
#   - --key=moonie-key

# Set up git with key and domain.
# - name: 'gcr.io/cloud-builders/git'
#   entrypoint: 'bash'
#   args:
#   - '-c'
#   - |
#     chmod 600 /root/.ssh/id_rsa
#     cat <<EOF >/root/.ssh/config
#     Hostname github.com
#     IdentityFile /root/.ssh/id_rsa
#     EOF
#     mv known_hosts /root/.ssh/known_hosts    

# For local test
# - name: 'gcr.io/cloud-builders/git'
#   entrypoint: 'bash'
#   args:
#   - '-c'
#   - |
#     mv id_rsa /root/.ssh/id_rsa
#     mv known_hosts /root/.ssh/known_hosts
#     cat <<EOF >/root/.ssh/config
#     Hostname github.com
#     IdentityFile /root/.ssh/id_rsa
#     EOF

# Clone code
# - name: 'gcr.io/cloud-builders/git'
#   dir: '/moonie'
#   args:
#   - clone
#   - git@github.com:minhhai2209/moonie

# Buid and deploy
- name: 'gcr.io/cloud-builders/npm'
  dir: '/moonie/moonie/functions'
  entrypoint: 'bash'
  args:
  - '-c'
  - |
    ls -al
    npm install 
    npm run build
    npm run deploy

options:
  logStreamingOption: STREAM_OFF
  sourceProvenanceHash: ['SHA256']
  diskSizeGb: 200
  logging: GCS_ONLY
  # volumes: [
  #   {
  #     name: 'moonie',
  #     path: /moonie
  #   }, 
  #   {
  #     name: 'moonie-ssh',
  #     path: /root/.ssh
  #   }
  # ]